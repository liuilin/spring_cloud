spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: localhost:8848 #配置中心地址
        # 指定 yaml 格式的配置，配置文件根据 spring.application.name 的名称 + 扩展名，
        # 会去 nacos 配置中心找 nacos-config-client.yaml 的文件
        file-extension: yaml
        namespace: 9269e8fc-49d3-48e2-b1d9-a3921614b3a5
#        group: