server:
  port: 8080
spring:
  application:
    name: cloud-consumer-order8080
eureka:
 client:
   register-with-eureka: true
   fetchRegistry: true
   service-url:
     defaultZone: http://localhost:7001/eureka